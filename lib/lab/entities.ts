import { EPOCH, iso } from './random';
import type { Host, LabUser } from './types';
export const departments = ['Finance', 'Engineering', 'Human Resources', 'Operations', 'Sales', 'IT', 'Legal', 'Marketing'];
const prefixes = ['FIN', 'DEV', 'HR', 'OPS', 'SAL', 'IT', 'LGL', 'MKT'], first = ['Riya', 'James', 'Aarav', 'Sarah', 'Neha', 'Daniel', 'Meera', 'Liam', 'Priya', 'Ethan', 'Ananya', 'Omar', 'Kavya', 'Noah', 'Isha', 'Oliver', 'Maya', 'Lucas', 'Zara', 'Arjun'], last = ['Patel', 'Smith', 'Kumar', 'Wilson', 'Sharma', 'Chen'];
export const users: LabUser[] = Array.from({ length: 120 }, (_, i) => { const f = first[i % 20], l = last[Math.floor(i / 20)]; return { id: `${f[0].toLowerCase()}.${l.toLowerCase()}${Math.floor(i / 20) === 0 && !first.slice(0, i % 20).some(x => x[0] === f[0]) ? '' : i}`, fullName: `${f} ${l}`, department: departments[i % 8], role: i % 17 === 0 ? 'Systems administrator' : i % 8 === 1 ? 'Software engineer' : i % 8 === 0 ? 'Finance analyst' : 'Business associate', manager: `${first[(i + 3) % 20]} ${last[(Math.floor(i / 20) + 1) % 6]}`, normalCountry: i % 9 === 0 ? 'United Kingdom' : i % 11 === 0 ? 'United States' : 'India', normalHours: '09:00–18:00 local · Monday–Friday', normalHost: `${prefixes[i % 8]}-${i % 3 === 0 ? 'LT' : 'WS'}-${String(i + 1).padStart(3, '0')}`, email: `${f.toLowerCase()}.${l.toLowerCase()}${i}@northstar.test`, groups: [departments[i % 8], 'Employees', ...(i % 17 === 0 ? ['IT Operations'] : [])], risk: 10 + (i * 7) % 40 }; });
export const hosts: Host[] = Array.from({ length: 240 }, (_, i) => { const u = users[i % 120], server = i >= 210; return { id: i < 120 ? u.normalHost : server ? ['DC01', 'DC02', 'VPN-GW-01', 'MAIL-GW-01', 'FW-EDGE-01', 'PROXY-01'][i - 210] ?? `${i % 3 === 0 ? 'WEB' : i % 3 === 1 ? 'SQL' : 'FILE'}-SRV-${String(i - 210).padStart(2, '0')}` : `${prefixes[i % 8]}-WS-${String(i + 1).padStart(3, '0')}`, user: u.id, os: [223,226,229,232].includes(i) ? 'Ubuntu Server 24.04 LTS' : server ? 'Windows Server 2022' : 'Windows 11 Enterprise', ip: `10.40.${Math.floor(i / 60) + 1}.${i % 60 + 10}`, mac: `02:40:${Math.floor(i / 60).toString(16).padStart(2, '0')}:00:00:${i.toString(16).padStart(2, '0')}`, department: server ? 'IT' : u.department, type: server ? 'Server' : i % 3 === 0 ? 'Laptop' : 'Workstation', lastSeen: iso(EPOCH - (i % 19) * 60000), edrStatus: i % 47 === 0 ? 'Sensor delayed' : 'Healthy', risk: 12 + (i * 13) % 63 }; });
